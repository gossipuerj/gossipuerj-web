import "package:flutter/material.dart";

class GlossipColors {
  // Core Gossip UERJ palette.
  static const topbarYellow = Color(0xFFFFFF00);
  static const bubblePink = Color(0xFFFF007F);
  static const hoverCardBlue = Color(0xFF00FFFF);

  static const primary = Color(0xFFFF00FF);
  static const secondary = hoverCardBlue;
  static const accent = topbarYellow;
  static const background = bubblePink;
  static const card = Colors.white;
  static const black = Colors.black;
}
