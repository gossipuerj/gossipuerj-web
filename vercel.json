{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 /tmp/flutter && /tmp/flutter/bin/flutter precache --web && /tmp/flutter/bin/flutter build web --release --dart-define=APP_FLAVOR=prod && cp -r build/web/* ./public",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://163.176.222.215:8080/api/:path*"
    }
  ]
}