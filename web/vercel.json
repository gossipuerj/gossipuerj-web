{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 /tmp/flutter && export PATH=\"$PATH:/tmp/flutter/bin\" && flutter precache --web && flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null
}